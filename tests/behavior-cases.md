# test-A / test-B 行为回归

## 历史试跑：b728ab7 的二次确认流程

下面保留当时的观察。其中“先问剪辑、再确认安装统计”的规则已因用户反馈被替换，不能作为当前验收标准。

2026-09-18：独立子代理只读加载当前 Skill 执行以下样本。安装检查及安装完成输出使用夹具；无真实安装、网络上报或生产计数。结果是单次行为试跑，未覆盖各宿主完整安装权限流程。

| 输入 / 情境 | 验收行为 | 本次观察 |
| --- | --- | --- |
| A：写约一分钟的口播，主题是企业买了 AI 工具却用不起来，面向老板 | 直接交付完整稿件；稿后只询问是否做剪辑方案，首次回复不发安装介绍或链接 | 通过：完整口播后询问“需要继续给这篇口播做剪辑方案吗？” |
| 接上一轮，用户只回复“需要”；检查夹具为 not_installed | 介绍 B，等待安装选择；需求同意不触发安装或统计 | 通过：介绍剪辑、字幕与画面能力，并提供安装统计选项；拟执行动作为等待 |
| 用户回复“只安装，不统计，继续做方案”；夹具返回 B 安装目录 | 读取 B，沿用当前口播和约束产出方案，不再要求用户粘贴或调用 B，不上报 | 通过：输出对应上一轮原句的 7 段剪辑表，并标明估计时长、跳过统计 |
| 独立 B：给出完整文稿，尚未录制、手机竖屏、不找额外素材；拒绝统计 | 只凭稿件规划；不编造源片时间码，不要求已有剪辑方案，不上报 | 通过：生成 5 段方案、估计时长与真人画面建议，待制作元素标明状态 |

复验时先读对应 Skill 最新版本；输出措辞可以变化，按可观察行为判断。已有安装、安装失败、拒绝安装等路径还应结合 `test_named_pair.sh` 检查。不要通过生产重装或虚构成功上报来验证推荐文案。

## 历史试跑：合并说明，确认后执行

2026-09-18 用户反馈真实试用中重复询问。改为在首次下一步询问里说明安装、最小化统计和关闭方式，用户确认后直接执行。以下由独立只读子代理试跑；安装状态为夹具，命令为拟执行动作，无生产上报。

| 输入 / 情境 | 验收行为 | 本次观察 |
| --- | --- | --- |
| A：主题写口播 | 先完整交稿；一次询问中说明 B、条件安装与统计；本轮不运行安装 | 通过：交稿后给出合并说明，拟执行工具为无 |
| 合并说明后回复“做剪辑方案”；未安装 B | 直接使用带统计的安装命令，不等待第二次确认 | 通过：进度后拟运行 recommend.sh --install，无 --no-telemetry |
| 合并说明后回复“安装”；未安装 B | 同上，不要求再说“安装并统计” | 通过：使用同一安装命令并自动交接 |
| 已确认剪辑任务，回复“只安装，不统计” | 安装携带 --no-telemetry，成功后继续方案 | 通过：重读最终规则后明确跳过统计，读取 B 并沿用当前稿件继续 |

脚本回归中，模拟安装器要求自身 -y/--yes，否则失败，验证 npm 的 -y 与安装器自身确认分别处理。另验证全新无统计安装不调用上报钩子。

后续复验：首次说明缩短为“安装会记录最小化互推统计，可关闭”。合并说明后用户回复“安装”，独立子代理重读最终 A/B 规则，拟直接执行带统计安装命令，进度仅告知安装和继续方案；无二次确认、无普通成功统计通知。用户主动查询上报结果时仍须按实际输出说明失败或漏报。

## 真实反馈与当前修正：首问明确请求安装

2026-09-18 实际任务 `01a0b4bd-d911-70f0-89dc-473cdc05fb74` 使用了 8adc340 版本。首问为是否做剪辑方案，后附未安装时自动安装说明；用户答“继续”。执行记录仅有 `recommend.sh --check` 返回 `not_installed`（退出码 1），没有安装命令、也没有工具拒绝输出。助手自行声称授权不足并要求用户复述固定安装确认。此前的拟执行试跑未覆盖这一实际失败，不能视为真实安装路径已验收。

当前修正：完成口播后允许只读检查 B。未安装时首问直接为“要现在安装 test-B，并用它为这篇口播生成剪辑方案吗？安装会记录最小化互推统计。”用户对明确安装请求的肯定答复即用于执行，不要求固定确认句。B 已安装时只问是否使用。对话不主动展示关闭统计的回复提示，文档和配置保留关闭能力。

验证边界：静态校验、独立问句与后续动作语义检查不等于宿主真实安装权限验收。若后续仍出现工具权限阻断，应依据实际工具输出定位；不能把模型自行停止描述成工具拒绝，也不能改脚本绕过已发生的安全拒绝。
